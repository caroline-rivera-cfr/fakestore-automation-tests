image: cypress/browsers:node20.9.0-chrome120-ff119

stages:
  - test

e2e:
  stage: test
  script:
    - npm ci
    - npx cypress run
  artifacts:
    when: always
    paths:
      - cypress/videos/**/*
      - cypress/screenshots/**/*
    expire_in: 1 week
